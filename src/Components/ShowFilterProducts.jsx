// import React from "react";
// import ProductData from "../ProductData";

// const ShowFilterProducts = ({ currVal, currFun }) => {
//   let finalAnswer;
//   let filteredData = ProductData.filter((val) => val.category === currVal);
//   return <>{filteredData.map(currFun())}</>;
// };

// export default ShowFilterProducts;
